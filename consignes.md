Au choix :

1) (Canvas) Améliorer les fonctionnalités du dessin
- Permettre à l'utilisateur de pouvoir choisir la couleur de son tracé (Attention : penser à l'envoyer au serveur)
- Permettre à l'utilisateur de pouvoir choisir la taille de son tracé (Attention : penser à l'envoyer au serveur)
OU
- Attribuer de base à chaque utilisateur une épaisseur et une couleur de tracé. 

2) Améliorer l'UX et l'UI de votre interface en utilisant la librairie DaisyUI et/ou du CSS custom.

3) (Canvas) Améliorer l'affichage des tracés 
- Utiliser la méthode canvas "quadraticCurveTo" (https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/quadraticCurveTo) pour obtenir des tracés moins "droits".

4) (React/Store) Ajouter une gestion de dark/light theme 
- Ajouter un store Zustand pour gérer le changement et la sauvegarde du thème
- Ajouter un bouton de switch 

